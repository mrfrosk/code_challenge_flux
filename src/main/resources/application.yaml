spring:
  application:
    name: CodeChallengeFlux
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  exposed:
    generate-ddl: true
